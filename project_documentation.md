# توثيق مشروع المتجر الإلكتروني 🛒

هذا الملف يوضح المعمارية (Architecture)، التقنيات، المكتبات، وواجهات برمجة التطبيقات (APIs) التي تم استخدامها في بناء المتجر الإلكتروني.

---

## 1. التقنيات الأساسية (Core Technologies) 🛠️
- **React.js**: إطار العمل الأساسي لبناء واجهة المستخدم بناءً على المكونات (Components).
- **React Router DOM (v6)**: يُستخدم لإدارة التوجيه (Routing) والتنقل بين الصفحات المختلفة دون إعادة تحميل الصفحة (Single Page Application).
- **Vanilla CSS**: تم استخدام CSS النقي لعمل التصميمات (Styling) وإضافة تأثيرات (Animations) وحركات جذابة للمتجر.
- **Firebase**: كخلفية (Backend-as-a-Service) لإدارة قواعد البيانات والمصادقة.

---

## 2. إدارة الحالة (State Management) 🧠
تم الاعتماد بشكل كامل على **React Context API** لربط وتمرير البيانات بين المكونات بسهولة، دون الحاجة لمكتبات خارجية مثل Redux.

تم تقسيم الـ Contexts لتنظيم البيانات كالتالي:
- **`UserContext`**: لإدارة بيانات المستخدم (تسجيل الدخول، تسجيل الخروج، حفظ وقراءة معلومات المستخدم، جلب الطلبات، وحفظها).
- **`CartContext`**: لإدارة سلة المشتريات (إضافة، إزالة، زيادة/نقصان الكمية، وتفريغ السلة)، مع مزامنة السلة مع الـ `localStorage` و Firestore.
- **`WishlistContext`**: لإدارة المنتجات المفضلة للمستخدمين ومزامنتها.
- **`LanguageContext`**: لتقديم دعم متعدد اللغات (الإنجليزية / العربية) وحفظ تفضيل المستخدم في `localStorage`، مع تحويل اتجاه الصفحة (RTL/LTR) تلقائياً.
- **`ToastContext`**: لإدارة إشعارات النظام السريعة (Toast Alerts) للمستخدم (مثل: "تمت الإضافة للسلة").

---

## 3. قواعد البيانات والمصادقة (Backend & Database) ☁️
تم استخدام **Firebase** من جوجل لتوفير خدمات الخلفية:
- **Firebase Authentication**: (إن وُجد) للتعامل مع تسجيل الدخول بكلمة المرور أو حسابات جوجل.
- **Firebase Firestore (NoSQL)**: 
  - **جدول `users`**: تخزين بيانات المستخدم (الاسم، العنوان، الهاتف). يتم إنشاء مجاميع فرعية (Subcollections) تحته مثل `orders` (الطلبات)، `cart` (مزامنة السلة)، و `wishlist` (المفضلة).
  - **جدول `products`**: لتتبع المخزون أو حفظ منتجات المتجر وتعديلها (كما يظهر في لوحة تحكم الأدمن لتقليل الكمية عند إتمام الشراء).

---

## 4. واجهات برمجة التطبيقات (APIs) المُستخدمة 🌐
تم الاعتماد على API مجاني لجلب المنتجات والبيانات الوهمية لبناء واجهة المتجر:
- **[DummyJSON API](https://dummyjson.com/)**:
  - `https://dummyjson.com/products`: لجلب كافة المنتجات.
  - `https://dummyjson.com/products/category/{categoryName}`: لجلب منتجات قسم معين.
  - `https://dummyjson.com/products/search?q={query}`: للبحث عن المنتجات بالاسم.
  - `https://dummyjson.com/products/{id}`: لجلب تفاصيل منتج معين.

---

## 5. بوابة الدفع (Payment Gateway) 💳
- **Stripe**: تم ربط الموقع ببوابة الدفع Stripe لمعالجة بطاقات الائتمان.
  - المكتبات المُستخدمة: `@stripe/stripe-js` و `@stripe/react-stripe-js`.
  - المكون المستخدم: يتم حقن الـ `Elements` و `CardNumberElement` داخل صفحة `Checkout.jsx` وتشفير البيانات وإرسالها بأمان.

---

## 6. المكتبات المساعدة (Libraries & Dependencies) 📦
- **`react-icons`**: مكتبة أيقونات ضخمة تحتوي على (FontAwesome, MaterialIcons وغيرها). تم استخدامها بكثافة لإضافة أيقونات جذابة في الأزرار (مثل `FaTrash`, `FaCheckCircle`, `FaShoppingCart`).
- **`react-router-dom`**: لإدارة الروابط (`<Link>`, `useNavigate`, `useLocation`).
- **`firebase`**: الأداة الرسمية للتواصل مع خدمات Firebase.
- **`@stripe/react-stripe-js`**: للتعامل الآمن مع المدفوعات.

---

## 7. آلية عمل وربط الأجزاء ببعضها (Project Flow) 🔄
1. **التهيئة (Initialization)**:
   - عند فتح الموقع، يتم فحص الـ `localStorage` لمعرفة اللغة المحفوظة، وتطبيقها من خلال `LanguageContext`.
   - يتم التأكد مما إذا كان المستخدم مسجلاً للدخول عبر `UserContext`. في حالة تسجيل الدخول، يتم جلب بيانات سلة التسوق والمفضلة والطلبات من Firestore.
2. **التصفح (Browsing)**:
   - المستخدم يتصفح الصفحات (`Home`, `Category`, `Search`). يتم إرسال طلب `fetch` إلى `DummyJSON` بناءً على تصنيف الصفحة.
   - المنتجات تُعرض في شبكة (Grid) أو شريط تمرير (Slider) مع إمكانية إضافة المنتج للسلة، للمفضلة، أو للمقارنة.
3. **سلة التسوق (Cart)**:
   - عند الإضافة للسلة، يتدخل `CartContext` ليقوم بإضافة العنصر للـ State، ويقوم بتخزينه فوراً في الـ `localStorage`. إذا كان المستخدم مسجلاً للدخول، يتم مزامنة السلة برفعها إلى `Firestore` لحفظها سحابياً.
   - يتوفر في السلة نظام أكواد خصم (Promo Codes) مثل `F1` أو `SAVE10`.
4. **إتمام الطلب (Checkout)**:
   - يقرأ صفحة الإتمام بيانات المستخدم والسلة.
   - يمكن الدفع عند الاستلام (COD) أو الدفع بالبطاقة الائتمانية عبر (Stripe).
   - عند تأكيد الطلب:
     1. يتم خصم كمية المنتجات المباعة من `Firestore` (جدول المنتجات).
     2. يُحفظ الطلب برقم تتبع عشوائي في مجلد `orders` الخاص بالمستخدم في `Firestore`.
     3. تُفرّغ سلة المشتريات.
5. **النجاح والتتبع**:
   - يتم توجيه المستخدم لصفحة `OrderSuccess` حيث تعمل مؤثرات بصرية (Confetti) لتهنئته.
   - يمكن للمستخدم بعد ذلك الذهاب لـ `Dashboard` لتتبع طلباته القديمة والجديدة وتعديل بياناته الشخصية.

---

## 8. ملاحظات إضافية على الجودة التنظيمية للمشروع 🚀
- **التنظيف الآمن (Clean Code)**: المشروع يلتزم بقواعد الـ Hooks الصارمة ولا يحتوي على متغيرات غير مستخدمة أو تسريبات للذاكرة، مع الحفاظ الكامل على حقوق مطور الكود (Student Code: 624018) في ترويسات الملفات.
- **تجاوب التصميم (Responsiveness)**: المتجر مصمم ليعمل على شاشات الهواتف والأجهزة اللوحية والمكتبية بانسيابية.
